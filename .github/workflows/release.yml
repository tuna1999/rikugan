name: Release

on:
  push:
    tags:
      - 'v*'
      - '[0-9]*.[0-9]*'
  workflow_dispatch:
    inputs:
      tag:
        description: 'Tag name to re-release (vd: v1.2 hoặc 1.2.3)'
        required: true
        type: string

permissions:
  contents: write

jobs:
  verify:
    name: Verify (tag + version + tests)
    runs-on: ubuntu-latest
    outputs:
      tag: ${{ steps.meta.outputs.tag }}
      version: ${{ steps.meta.outputs.version }}
      is_prerelease: ${{ steps.meta.outputs.is_prerelease }}
    steps:
      - uses: actions/checkout@v7
        with:
          fetch-depth: 0

      - uses: actions/setup-python@v6
        with:
          python-version: "3.11"

      - name: Install dev tools + runtime deps
        run: |
          pip install ruff mypy pytest tomli desloppify
          pip install -r requirements.txt
          pip install PySide6

      - name: Parse tag, version, pre-release flag
        id: meta
        run: |
          set -e
          if [ "${{ github.event_name }}" = "workflow_dispatch" ]; then
            TAG="${{ inputs.tag }}"
          else
            TAG="${GITHUB_REF_NAME}"
          fi

          VERSION="${TAG#v}"

          # Version must agree in ALL THREE sources, per the release flow
          # documented in CLAUDE.md ("Bump version ở cả 3 nguồn (sync)").
          # Catches drift between ida-plugin.json (HCLI metadata),
          # pyproject.toml (PEP 621), and rikugan/constants.py (runtime
          # PLUGIN_VERSION shown in UI / logs).
          PLUGIN_JSON_VERSION=$(python -c "import json; print(json.load(open('ida-plugin.json'))['plugin']['version'])")
          PYPROJECT_VERSION=$(python -c "import tomllib; print(tomllib.load(open('pyproject.toml','rb'))['project']['version'])")
          CONSTANTS_VERSION=$(python -c "
          import re
          src = open('rikugan/constants.py').read()
          m = re.search(r'^PLUGIN_VERSION\\s*=\\s*[\"\\']([^\"\\']+)[\"\\']', src, re.MULTILINE)
          print(m.group(1) if m else '')
          ")

          if [ "$VERSION" != "$PLUGIN_JSON_VERSION" ]; then
            echo "::error::Tag '$TAG' (→ version '$VERSION') does not match ida-plugin.json '$PLUGIN_JSON_VERSION'"
            exit 1
          fi
          if [ "$VERSION" != "$PYPROJECT_VERSION" ]; then
            echo "::error::Tag '$TAG' (→ version '$VERSION') does not match pyproject.toml '$PYPROJECT_VERSION' — bump pyproject.toml to '$VERSION'"
            exit 1
          fi
          if [ "$VERSION" != "$CONSTANTS_VERSION" ]; then
            echo "::error::Tag '$TAG' (→ version '$VERSION') does not match rikugan/constants.py PLUGIN_VERSION '$CONSTANTS_VERSION' — bump constants.py to '$VERSION'"
            exit 1
          fi

          if [[ "$TAG" =~ -(rc|alpha|beta|pre|dev)[0-9]*$ ]]; then
            IS_PRERELEASE="true"
          else
            IS_PRERELEASE="false"
          fi

          echo "tag=$TAG" >> "$GITHUB_OUTPUT"
          echo "version=$VERSION" >> "$GITHUB_OUTPUT"
          echo "is_prerelease=$IS_PRERELEASE" >> "$GITHUB_OUTPUT"
          echo "::notice::Parsed: tag=$TAG, version=$VERSION (synced in ida-plugin.json, pyproject.toml, rikugan/constants.py), is_prerelease=$IS_PRERELEASE"

      - name: Re-run CI checks (inline)
        run: |
          set -e
          echo "── ruff format check ──"
          python -m ruff format --check rikugan/

          echo "── ruff lint ──"
          python -m ruff check rikugan/

          echo "── mypy ──"
          python -m mypy rikugan/core rikugan/providers

          echo "── pytest ──"
          python -m pytest tests/ --tb=short -q

          echo "── desloppify (objective score) ──"
          desloppify scan --profile objective --no-badge
          SCORE=$(python -c "import json; print(json.load(open('.desloppify/query.json')).get('objective_score', 0))")
          BASELINE=89.0
          python -c "
          import sys
          s = float('$SCORE')
          if s < $BASELINE - 0.5:
              sys.exit(f'score {s} < baseline {$BASELINE} - 0.5')
          print(f'OK — score {s}')
          "

  build:
    name: Build + validate artifacts
    needs: verify
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v7

      - uses: actions/setup-python@v6
        with:
          python-version: "3.11"

      - name: Run build_release.py
        run: |
          python scripts/build_release.py \
            --version "${{ needs.verify.outputs.version }}" \
            --out-dir dist

      - name: HCLI packaging validation (shim fallback)
        run: |
          # Prefer hcli plugin lint if available; else run the Python shim.
          if command -v hcli >/dev/null 2>&1; then
            echo "hcli found — running 'hcli plugin lint'"
            hcli plugin lint "dist/rikugan-v${{ needs.verify.outputs.version }}.zip"
          else
            echo "hcli not found — running structural shim"
            python scripts/validate_archive.py "dist/rikugan-v${{ needs.verify.outputs.version }}.zip"
          fi

      - name: Upload artifacts
        uses: actions/upload-artifact@v7
        with:
          name: release-artifacts
          path: dist/
          if-no-files-found: error

  publish:
    name: Publish GitHub Release
    needs: [verify, build]
    runs-on: ubuntu-latest
    steps:
      - name: Download build artifacts
        uses: actions/download-artifact@v8
        with:
          name: release-artifacts
          path: dist/

      - name: Create or update GitHub Release
        uses: softprops/action-gh-release@v3
        with:
          tag_name: ${{ needs.verify.outputs.tag }}
          name: "Rikugan ${{ needs.verify.outputs.version }}"
          prerelease: ${{ needs.verify.outputs.is_prerelease == 'true' }}
          generate_release_notes: true
          fail_on_unmatched_files: true
          files: |
            dist/rikugan-v${{ needs.verify.outputs.version }}.zip
            dist/SHA256SUMS
